wsl --install -d Ubuntu                                                             #check python packages  
                                                                                    pip list

cd ~                                                                        

sudo apt update && sudo apt upgrade -y
sudo apt update
sudo apt install python3-venv -y                                                #start airflow webserver
                                                                                airflow webserver --port 8080
python3 -m venv venv

source venv/bin/activate                                                    #create airflow users
                                                                            airflow users create --username harsh --role Admin --firstname harsh --lastname k --email harsh@k.com
                                                                            password is Bappa@143
(venv) harshini@LAPTOP-xxxx:~$

pip install mlflow apache-airflow==2.10.5 pandas scikit-learn               #open 3 bash scripts -> one for webserver, second for scheduler, third for mlflow
                                                                            Remember airflow doesnt remember users unless we use a proper backend such as PostgresSQL. 
                                                                            So gotta create a new one everytime u login


## to Check where airflow has been installed
cd /
cd mnt

#now
which airflow
cd ~/airflow

#changing path??
cd~

#initializing bash script
nano ~/.bashrc

#creating a directory to store airflow so that I dont have to manually 

export AIRFLOW_HOME = /mnt/c/Users/harsh/fin/airflow

#pointing to correct folder for dags
export AIRFLOW__CORE__DAGS_FOLDER=/mnt/c/Users/harsh/fin/airflow_home/dags

/mnt/c/Users/harsh/fin/venv/bin/airflow